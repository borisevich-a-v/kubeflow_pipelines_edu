name: Get preprocess data
description: Gets images and text files from the input pdf file.

implementation:
  container:
    image: docker.io/tinker000/simple_pipeline:latest
    command: [ python, /preprocess/preprocess_run.py ]
